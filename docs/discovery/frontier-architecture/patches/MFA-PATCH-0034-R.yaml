# Copyright (c) 2026 STAVROPOULOS LAW. All Rights Reserved.
id: MFA-PATCH-0034-R
title: 'Rollback of MFA-PATCH-0034: Unknowns MFA-UNK-115…133 (EPISTEMIC/EXTERNAL_FACT, χωριστά από τον στόχο), Conflicts MFA-CFL-039…046 (με resolution condition),
  Research Programs MFA-RSP-021…040 (ένα ανά μηχανισμό + LWM-OEC για ELM-105/106, AA-020)'
operation: RESTORE
targets:
- MFA-CFL-039
- MFA-CFL-040
- MFA-CFL-041
- MFA-CFL-042
- MFA-CFL-043
- MFA-CFL-044
- MFA-CFL-045
- MFA-CFL-046
- MFA-RSP-021
- MFA-RSP-022
- MFA-RSP-023
- MFA-RSP-024
- MFA-RSP-025
- MFA-RSP-026
- MFA-RSP-027
- MFA-RSP-028
- MFA-RSP-029
- MFA-RSP-030
- MFA-RSP-031
- MFA-RSP-032
- MFA-RSP-033
- MFA-RSP-034
- MFA-RSP-035
- MFA-RSP-036
- MFA-RSP-037
- MFA-RSP-038
- MFA-RSP-039
- MFA-RSP-040
- MFA-UNK-115
- MFA-UNK-116
- MFA-UNK-117
- MFA-UNK-118
- MFA-UNK-119
- MFA-UNK-120
- MFA-UNK-121
- MFA-UNK-122
- MFA-UNK-123
- MFA-UNK-124
- MFA-UNK-125
- MFA-UNK-126
- MFA-UNK-127
- MFA-UNK-128
- MFA-UNK-129
- MFA-UNK-130
- MFA-UNK-131
- MFA-UNK-132
- MFA-UNK-133
base:
  package_version: 0.1.0
  file_hashes:
    CONFLICTS.yaml: 0dd1c37bb75d32a1e7213516d1bd0e68f770c541f3e090774e3ecd07a1da7275
    RESEARCH-PROGRAMS.yaml: 1e0c610a1ff4d822cba7ac31634a30273028a4c844ead2fdd00819b315ab0922
    UNKNOWNS.yaml: fcc376f1d2e856bba21187bbc3ae2591b87c48ef77c239e7d6c0cd1a27d578e1
preconditions:
- MFA-PATCH-0034.status == APPLIED
change:
  MFA-UNK-115:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-116:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-117:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-118:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-119:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-120:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-121:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-122:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-123:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-124:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-125:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-126:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-127:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-128:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-129:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-130:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-131:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-132:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-UNK-133:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-CFL-039:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-CFL-040:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-CFL-041:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-CFL-042:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-CFL-043:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-CFL-044:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-CFL-045:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-CFL-046:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-021:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-022:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-023:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-024:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-025:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-026:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-027:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-028:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-029:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-030:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-031:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-032:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-033:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-034:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-035:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-036:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-037:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-038:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-039:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-RSP-040:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
rationale: Αντίστροφο του MFA-PATCH-0034, γραμμένο μαζί (PATCH-PROTOCOL §3)· επαναφέρει τις προηγούμενες τιμές των πεδίων· οι ADD εγγραφές μένουν με status WITHDRAWN
  (append-only).
evidence:
- καμία — μηχανική αντιστροφή
affected_dependencies:
- MFA-ELM-104 (RVSI καταναλωτής)
- MFA-ELM-086 (HELD_OUT)
- MFA-ELM-099 (VFC)
capability_impact:
  gained: []
  at_risk:
  - MFA-CAP-089
  - MFA-CAP-090
  - MFA-CAP-091
  - MFA-CAP-092
  - MFA-CAP-093
  - MFA-CAP-094
  - MFA-CAP-095
  - MFA-CAP-096
  - MFA-CAP-097
  - MFA-CAP-098
  - MFA-CAP-099
  - MFA-CAP-100
  - MFA-CAP-101
  - MFA-CAP-102
  - MFA-CAP-103
  - MFA-CAP-104
  - MFA-CAP-105
  - MFA-CAP-106
  - MFA-CAP-107
  - MFA-CAP-108
  ledger_entry: CAPABILITY-CONSERVATION.md §0.4.0 — 20 νέες ικανότητες, 0 αφαιρέσεις
migration: ό,τι αναφέρεται στα νέα πεδία επιστρέφει στα παλιά· νέα IDs μένουν WITHDRAWN
verification: tools/check-package.py
rollback: MFA-PATCH-0034
author: Stavropoulos Law® — Pass H (regeneration after Pass G), 2026-09-16
status: PROPOSED
