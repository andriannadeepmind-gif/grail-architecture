# Copyright (c) 2026 STAVROPOULOS LAW. All Rights Reserved.
id: MFA-PATCH-0032-R
title: 'Rollback of MFA-PATCH-0032: Invariants MFA-INV-078…115 (δύο ανά μηχανισμό· Tier-0 όσες φρουρούν το σταθερό σημείο, τη γραμμικότητα υποχρεώσεων, τη διατήρηση
  Telos, τις διακοπές)'
operation: RESTORE
targets:
- MFA-INV-078
- MFA-INV-079
- MFA-INV-080
- MFA-INV-081
- MFA-INV-082
- MFA-INV-083
- MFA-INV-084
- MFA-INV-085
- MFA-INV-086
- MFA-INV-087
- MFA-INV-088
- MFA-INV-089
- MFA-INV-090
- MFA-INV-091
- MFA-INV-092
- MFA-INV-093
- MFA-INV-094
- MFA-INV-095
- MFA-INV-096
- MFA-INV-097
- MFA-INV-098
- MFA-INV-099
- MFA-INV-100
- MFA-INV-101
- MFA-INV-102
- MFA-INV-103
- MFA-INV-104
- MFA-INV-105
- MFA-INV-106
- MFA-INV-107
- MFA-INV-108
- MFA-INV-109
- MFA-INV-110
- MFA-INV-111
- MFA-INV-112
- MFA-INV-113
- MFA-INV-114
- MFA-INV-115
base:
  package_version: 0.1.0
  file_hashes:
    INVARIANTS.yaml: 7cf4c3381a72ca225596c33ccae9d2d7be45f79a88a0479663bcff8baa5bc7c2
preconditions:
- MFA-PATCH-0032.status == APPLIED
change:
  MFA-INV-078:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-079:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-080:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-081:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-082:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-083:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-084:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-085:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-086:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-087:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-088:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-089:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-090:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-091:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-092:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-093:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-094:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-095:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-096:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-097:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-098:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-099:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-100:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-101:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-102:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-103:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-104:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-105:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-106:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-107:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-108:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-109:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-110:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-111:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-112:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-113:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-114:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
  MFA-INV-115:
    status: WITHDRAWN
    _note: rollback of ADD keeps the record (append-only) and withdraws it
rationale: Αντίστροφο του MFA-PATCH-0032, γραμμένο μαζί (PATCH-PROTOCOL §3)· επαναφέρει τις προηγούμενες τιμές των πεδίων· οι ADD εγγραφές μένουν με status WITHDRAWN
  (append-only).
evidence:
- καμία — μηχανική αντιστροφή
affected_dependencies:
- MFA-ELM-104 (RVSI καταναλωτής)
- MFA-ELM-086 (HELD_OUT)
- MFA-ELM-099 (VFC)
capability_impact:
  gained: []
  at_risk:
  - MFA-CAP-089
  - MFA-CAP-090
  - MFA-CAP-091
  - MFA-CAP-092
  - MFA-CAP-093
  - MFA-CAP-094
  - MFA-CAP-095
  - MFA-CAP-096
  - MFA-CAP-097
  - MFA-CAP-098
  - MFA-CAP-099
  - MFA-CAP-100
  - MFA-CAP-101
  - MFA-CAP-102
  - MFA-CAP-103
  - MFA-CAP-104
  - MFA-CAP-105
  - MFA-CAP-106
  - MFA-CAP-107
  - MFA-CAP-108
  ledger_entry: CAPABILITY-CONSERVATION.md §0.4.0 — 20 νέες ικανότητες, 0 αφαιρέσεις
migration: ό,τι αναφέρεται στα νέα πεδία επιστρέφει στα παλιά· νέα IDs μένουν WITHDRAWN
verification: tools/check-package.py
rollback: MFA-PATCH-0032
author: Stavropoulos Law® — Pass H (regeneration after Pass G), 2026-09-16
status: PROPOSED
